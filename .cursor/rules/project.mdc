---
description: Regras do projeto FrontEnd-Architecture - convencoes, padroes e arquitetura
globs: *
alwaysApply: true
---
# Project Rules

## Stack
React 19 + Vite 7 + TanStack Router + TanStack Query + Zustand + ShadCN UI + TailwindCSS 4 + react-hook-form + zod + Biome

## Regras Criticas

- Use ShadCN UI (`src/components/ui`) - NUNCA tags HTML puras estilizadas
- Usar Zustand com `persist` - NUNCA `localStorage.setItem` direto
- PROIBIDO usar `.` para criar rotas aninhadas (ex: `edit.$id.tsx`)
- NUNCA usar Card em componentes comuns - usar `Item`, `ItemTitle`, `ItemDescription`
- NUNCA `mx-auto` no ChartContainer. Cores: `getChartColor(index)`
- ANTES de criar hooks, verificar se ja existe em `src/hooks/`
- Antes de commitar: `pnpm run format` e `pnpm run check`

## Componentes Obrigatorios
- `<DefaultEmptyData />`, `<DefaultLoading />`, `<DefaultFormLayout />`

## Estrutura de Pagina
```tsx
<Card>
  <CardHeader>{/* acoes */}</CardHeader>
  <CardContent>{/* conteudo */}</CardContent>
  <CardFooter layout="multi | single">{/* paginacao */}</CardFooter>
</Card>
```

## Estrutura de Pastas da Rota
```
src/routes/_private/{module}/
├── index.tsx, add.tsx, $id.tsx
├── @components/, @consts/, @hooks/, @interface/, @utils/
```

## Documentacao Detalhada

Para padroes detalhados com exemplos, consulte `docs/` conforme o cenario:
- Nova feature / criar pastas: `docs/route-structure.md`, `docs/checklist.md`
- Paginas de listagem ou formulario: `docs/page-patterns.md`
- Hooks de API (TanStack Query): `docs/api-hooks.md`
- Formularios (react-hook-form): `docs/form-hooks.md`
- Schemas Zod, interfaces, consts: `docs/schemas-types.md`
- Estado global (Zustand): `docs/state-management.md`
- Padroes React e anti-patterns: `docs/react-patterns.md`
- Graficos (Recharts, cores, tipos): `docs/charts.md`
- Selects, hooks e componentes existentes: `docs/available-resources.md`
- Máscaras e Funções de Utilidade (CPF, Tel, Data): `docs/masks.md`
- Tech stack e versoes: `docs/stack.md`
