import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ge-cloud',
  appName: 'GE-Cloud',
  webDir: 'dist',
  zoomEnabled: false,
};

export default config;
