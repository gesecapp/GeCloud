import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gesec',
  appName: 'GeCloud',
  webDir: 'dist',
  zoomEnabled: false,
};

export default config;
